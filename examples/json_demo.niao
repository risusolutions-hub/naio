// JSON parse/stringify demo — import native json module.
import "json"

let payload = "{\"name\":\"Niao\",\"values\":[1,2,3],\"ok\":true}"
let obj = json.parse(payload)
print("name:", json.get(obj, "name"))
print("stringify:", json.stringify(obj))
print("valid:", json.valid(payload))
