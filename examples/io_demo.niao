// Async I/O foundation demo (executor + background tasks).
// Run:  niao run examples/io_demo.niao
import "io" as io

fn main() {
    let id = io.spawn_async(fn() {
        return 42
    })
    let v = io.await_task(id)
    print("spawn_async result:", v)
    print("io demo done")
}
