// Measures nmath scalar transcendental throughput (sqrt/sin/log hot loop).
import "nmath"
fn main() {
    let s = 0.0
    let i = 1
    while i < 1000000 {
        s = s + nmath.sqrt(nmath.abs(nmath.sin(i * 0.001)))
        i = i + 1
    }
    print(s)
}
