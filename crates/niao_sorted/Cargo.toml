[package]
name = "niao_sorted"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Sorted list / set / dict with bisect, range queries, and nearest lookup"

[lib]
bench = false

[[bin]]
name = "sorted_bench"
path = "src/bin/sorted_bench.rs"
