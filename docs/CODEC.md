# codec (legacy)

Encoding and canonicalization are covered by **`nfmt`** and **`ncanon`**. See [NFMT.md](NFMT.md) and [NCANON.md](NCANON.md).

`import "codec"` remains available for backward compatibility.
