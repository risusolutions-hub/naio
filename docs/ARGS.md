# args (legacy)

This crate-backed library is superseded by **`nargs`**. See [NARGS.md](NARGS.md).

`import "args"` remains available for backward compatibility; new code should use `import "nargs"`.
