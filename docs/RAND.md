# rand (legacy)

This crate-backed library is superseded by **`nrand`**. See [NRAND.md](NRAND.md).

`import "rand"` remains available for backward compatibility; new code should use `import "nrand"`.
