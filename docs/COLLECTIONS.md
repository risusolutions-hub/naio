# collections (legacy)

Maps, sets, and ordered structures live in **`dsa`**. See [DSA.md](DSA.md).

`import "collections"` remains available as a Rust-level backing crate; prefer `import "dsa"` in Niao code.
